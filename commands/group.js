groupAdd = 'mc admin group add ' + alias + ' ' + groupName + ' ' + userName
groupRemoveUser = 'mc admin group remove ' + alias + ' ' + groupName + ' ' + userName
groupRemove = 'mc admin group remove ' + alias + ' ' + groupName
groupInfo = 'mc admin group info ' + alias + ' ' + groupName
groupList = 'mc admin group list ' + alias
groupEnable = 'mc admin group enable ' + alias + ' ' + groupName
groupDisable = 'mc admin group disable ' + alias + ' ' + groupName
